from __future__ import annotations

"""Shared constants used across the backend."""

# RTD target-line suffix appended to line names to distinguish target from dev
TARGET_SUFFIX = "_TARGET"

# Sentinel value returned when rule/version catalog fetch fails
RULE_ERROR_ITEM = "error"
